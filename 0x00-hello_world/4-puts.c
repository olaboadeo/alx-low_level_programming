#include <stdio.h>
#include <string.h>

/**
  *main - main entry point
  *
  *Return: always 0
  */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
